<Window x:Class="SubRed.ExtractSubsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SubRed"
        mc:Ignorable="d"
        Title="ExtractSubsWindow" Height="552" Width="1440">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <Menu x:Name="menu">
                <MenuItem Header="Открыть изображение" Click="OpenImage_Click"></MenuItem>
                <MenuItem Header="Открыть видео" Click="OpenVideo_Click"></MenuItem>
            </Menu>
        </Grid>
        <Grid Grid.Column="0" Grid.Row="1">
            <DockPanel Width="250"/>
        </Grid>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition MinHeight="420" Height="*"/>
                <RowDefinition Height="31"/>
            </Grid.RowDefinitions>
            <Image Grid.Row="0" x:Name="image"/>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="0">
            <Button x:Name="runButton" Content="Run" HorizontalAlignment="Center" Height="30" VerticalAlignment="Center" Click="RunButton_Click" Width="52"/>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="1" Panel.ZIndex="1" MinHeight="100" MaxHeight="100">
            <DataGrid x:Name="SubtitleGrid" ItemsSource="{Binding Subtitle}" AutoGenerateColumns="False"
                        ScrollViewer.CanContentScroll="True" 
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Begin" Binding="{Binding begin}" Width="80"  FontFamily="Tahoma"></DataGridTextColumn>
                    <DataGridTextColumn Header="End" Binding="{Binding end}" Width="80"  FontFamily="Tahoma"></DataGridTextColumn>
                    <DataGridTextColumn Header="Duration" Binding="{Binding duration}" Width="60"  FontFamily="Tahoma"></DataGridTextColumn>
                    <DataGridTextColumn Header="FrameBeginNum" Binding="{Binding frameBeginNum}" Width="85"  FontFamily="Tahoma"></DataGridTextColumn>
                    <DataGridTextColumn Header="FrameEndNum" Binding="{Binding frameEndNum}" Width="85"  FontFamily="Tahoma"></DataGridTextColumn>
                    <DataGridTextColumn Header="Text" Binding="{Binding text}" Width="500"  FontFamily="Tahoma"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
